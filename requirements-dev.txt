# Optional dev dependencies — NOT needed by core or the eval gate (those use requirements.txt).
# matplotlib: eval/plot_history.py chart.  pytest: the unit suite.  langchain-community: the LangChain example.
matplotlib>=3.7
pytest>=7
langchain-community>=0.2
