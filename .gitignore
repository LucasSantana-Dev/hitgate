__pycache__/
*.pyc
*.sqlite
*.sqlite-wal
*.sqlite-shm
.DS_Store

# Local eval experiment outputs (ablations, one-off runs) are ignored.
# The per-corpus baseline.*.json files are explicitly un-ignored below because
# CI (check.sh) and run.py depend on them as frozen golden references.
hitgate/*.json
hitgate/candidates.jsonl
!hitgate/baseline.example.json
!hitgate/baseline.auto-rerank.json
!hitgate/baseline.fastapi.json
!hitgate/baseline.lucky.json
!hitgate/baseline.adt.json
!hitgate/baseline.criativaria.json
!hitgate/baseline.portfolio.json
!hitgate/baseline.forge-mcp.json
!hitgate/baseline.homelab.json

corpus-demo/.index/
*.egg-info/
build/
.venv/
dist/
